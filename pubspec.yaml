name: flutter_simple_image_editor
description: This packgae is simple image editor it've ability to flip , rotate , crop .
version: 1.0.1
homepage:

environment:
  sdk: ">=2.19.3 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  extended_image: 8.1.1
  image_editor: 1.3.0
  flutter_svg: 2.0.9
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/icons/

